<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>League of Legends Teams</title>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", media: "all" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true  %>
</head>
<body class="bg-gray-100">

  <!-- Menu de navigation -->
  <nav class="bg-blue-600 p-4">
    <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8">
      <div class="relative flex items-center justify-between h-16">
        <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-start">
          <div class="flex-shrink-0">
            <%= link_to "LoL Teams", root_path, class: "text-white text-xl font-semibold" %>
          </div>
          <div class="hidden sm:block sm:ml-6">
            <div class="flex space-x-4">
              <%= link_to "Équipes", teams_path, class: "text-white px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-700" %>
              <%= link_to "Joueurs", players_path, class: "text-white px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-700" %>
              <%= link_to "Matchs", matches_path, class: "text-white px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-700" %>
              <%= link_to "Classement", ranking_path, class: "text-white px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-700" %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </nav>

  <!-- Contenu principal -->
  <div class="min-h-screen bg-gray-50 py-6 px-4 sm:px-6 lg:px-8">
    <%= yield %> <!-- Le contenu spécifique à chaque page sera rendu ici -->
  </div>

  <footer class="fixed bottom-0 left-0 w-full bg-blue-600 text-white text-center py-3 shadow-md">
    <p class="text-sm">© <%= Time.current.year %> Nicolas De Bruycker</p>
  </footer>
</body>

</html>
